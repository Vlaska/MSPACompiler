@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '{text}'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    if arguments['name'] != '':
        out += '<{name}'
        if arguments['parent'] != '':
            out += ' {parent}'
        if arguments['class_'] != '':
            out += ' class="{class_}"'
        if arguments['style'] != '':
            out += ' style="{style}"'
        out += '>'
    else:
        "raise Exception('HTML tag name is required.')"

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '{text}</{name}>'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '<p>{text}</p>'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '  | [ | text | : | {id}: {text} | ] |   | [ | args | [ | class_ | ] | : | {tag_name} | ]'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '  | [ | text  | : | [ | code | [ | quirk | ] | : | {text} | ] | ] |   | [ | ifnot | [ | id | ] | : | [ | args | [ | id | ] | : | [ | upper | : | {tag_name} | ] | ] | ]'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += '{name} <span class="{class_}">\[{id}\]</span>'

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''

############
@classproperty
def text(
    cls,
    text: str,
    isTop: bool = False,
    isSafe: bool = True,
    **arguments
):
    out = ""
    arguments = {**self.arguments, **arguments}
    text = cls.textCleanup(text, isTop, isSafe)

    out += ''
